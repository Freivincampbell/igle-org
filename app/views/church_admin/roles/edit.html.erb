<section class="mx-auto w-full max-w-6xl px-6 py-8">
  <%= render "church_admin/shared/nav", church: @church %>

  <div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
    <div>
      <p class="text-xs font-semibold uppercase tracking-widest text-violet-500"><%= @church.name %></p>
      <h1 class="mt-1 text-2xl font-bold text-slate-900">Editar rol</h1>
    </div>

    <%= link_to "Ver rol", church_admin_role_path(@church, @role), class: "inline-flex items-center gap-1.5 rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50 transition-colors" %>
  </div>

  <%= render "form", role: @role %>

  <% if policy(@role).manage? %>
    <div class="mt-10 rounded-xl border border-slate-200 bg-white p-6 shadow-card">
      <div>
        <h2 class="text-base font-semibold text-slate-900">Matriz de permisos</h2>
        <p class="mt-1 text-sm text-slate-500">Selecciona las acciones permitidas para este rol.</p>
      </div>

      <%= render "permission_matrix", role: @role, permissions: @permissions, selected_permission_public_ids: @selected_permission_public_ids %>
    </div>
  <% end %>
</section>
