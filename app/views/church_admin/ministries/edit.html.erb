<section class="mx-auto w-full max-w-3xl px-6 py-8">
  <%= render "church_admin/shared/nav", church: @church %>

  <div>
    <p class="text-xs font-semibold uppercase tracking-widest text-violet-500"><%= @church.name %></p>
    <h1 class="mt-1 text-2xl font-bold text-slate-900">Editar ministerio</h1>
  </div>

  <%= render "form", ministry: @ministry %>
</section>
